/**
 * Vite Environment Declarations
 *
 * Responsibilities:
 * - Define external types provided by Vite (e.g., client types)
 * - Expose environment variables to the TypeScript context
 *
 * Boundaries:
 * - No build configuration (handled in vite.config.ts)
 */

/// <reference types="vite/client" />
