/**
 * Filters Feature Module
 *
 * Responsibilities:
 * - Entry point for local image results filtering
 * - Export filter hooks and UI components
 *
 * Boundaries:
 * - Does not perform server-side search operations
 */

export * from './components/LocalFilterPopup';
export * from './hooks/useLocalFilters';
